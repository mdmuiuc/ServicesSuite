namespace Services
{
    using System;

    public static class Utilities
    {
        /// <summary>
        /// Returns the first unique character in the string.
        /// If there are no unique characters, return null.
        /// </summary>
        public static char? FindFirstUnique(string input)
        {
            return null;
        }

        /// <summary>
        /// Returns the maximum value of all nodes in the tree.
        /// If the tree is empty, return int.MinValue.
        /// </summary>
        public static int GetTreeMax(Tree tree)
        {
            return int.MinValue;
        }
    }
}
